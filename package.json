{
  "scripts": {
    "server": "json-server --watch db.json --delay 1000 --host 0.0.0.0 --port 3001",
    "mobile:ios": "cd mobile && yarn run ios",
    "mobile:android": "cd mobile && yarn run android",
    "web": "cd web && yarn run dev"
  },
  "dependencies": {
    "json-server": "0.17.3"
  }
}
